import { Box, Typography } from "@mui/material";

const WelcomeChat = () => {
  
  return (
    <Box sx={{ flex: 1, display: "flex", justifyContent: "center" }}>
      <Typography>select a friend to start chat</Typography>
    </Box>
  );
};

export default WelcomeChat;
